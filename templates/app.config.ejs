/*
 * Copyright (c) 2015-2017 PointSource, LLC.
 * MIT Licensed
 */

(function () {
'use strict';

/* eslint-disable quotes */

angular.module('<%- moduleName %>'<% if (deps) { %>, <%= JSON.stringify(deps) %><% } %>)
<% constants.forEach(function(constant) { %>
.constant('<%- constant.name %>', <%= constant.value %>)
<% }) %>
;

angular.module('app').requires.push('<%= moduleName %>');

window.platformType = <%= JSON.stringify(JSON.parse(constants[0].value).platformType) %>;
}());
